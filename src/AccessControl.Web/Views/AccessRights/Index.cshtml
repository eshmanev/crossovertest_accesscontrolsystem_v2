@model AccessControl.Web.Models.AccessRights.AccessRightsIndexViewModel


<h3>Users' access rights</h3>
<table class="table table-hover table-bordered">
    <thead>
        <tr>
            <th rowspan="2">User</th>
            <th colspan="4">Access Rights</th>
            <th rowspan="2">#</th>
        </tr>
        <tr>
            <th>Type</th>
            <th>Site</th>
            <th>Department</th>
            <th>Access Point</th>
        </tr>
    </thead>
    <tbody>
    @foreach (var rights in Model.UserAccessRights)
    {
        <tr><td rowspan="@(2 + rights.PermanentAccessRules.Length + rights.ScheduledAccessRules.Length)">@Model.GetDisplayName(rights.UserName)</td></tr>
        @Html.Partial("PermanentPartial", new { Rules = rights.PermanentAccessRules, Owner = Model })
        @Html.Partial("ScheduledPartial", new { Rules = rights.ScheduledAccessRules, Owner = Model })
    }
    </tbody>
</table>
<br/>

<h3>User groups' access rights</h3>
<table class="table table-hover table-bordered">
    <thead>
        <tr>
            <th rowspan="2">User group</th>
            <th colspan="4">Access Rights</th>
            <th rowspan="2">#</th>
        </tr>
        <tr>
            <th>Type</th>
            <th>Site</th>
            <th>Department</th>
            <th>Access Point</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var rights in Model.UserGroupAccessRights)
        {
            <tr><td rowspan="@(2 + rights.PermanentAccessRules.Length)">@Model.GetDisplayName(rights.UserGroupName)</td></tr>
            @Html.Partial("PermanentPartial", new { Rules = rights.PermanentAccessRules, Owner = Model })
        }
    </tbody>
</table>
<br/>

@Html.Partial("EditorPartial")

@section scripts
{
    <script type="text/javascript" src="~/Scripts/access-rights-index.js"></script>
}
