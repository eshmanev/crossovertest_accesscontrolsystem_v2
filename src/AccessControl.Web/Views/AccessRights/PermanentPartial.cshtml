@using AccessControl.Web.Models.AccessRights
@model dynamic

@if (Model.Rules.Length > 0)
{
    <tr><td rowspan="@(2 + Model.Rules.Length)">Permanent:</td></tr>
    foreach (var item in Model.Rules)
    {
        <tr>
            <td>@Model.Owner.GetDepartment(item.AccessPointId)</td>
            <td>@Model.Owner.GetName(item.AccessPointId)</td>
            <td>@Html.ActionLink("deny", "Deny", new { userOrGroupName = Model.Identity, accessPointId = item.AccessPointId, group = Model.IsGroup })</td>
        </tr>
    }
}